# Work It Out

> 专业的论文项目管理平台 - 让学术写作更高效

Work It Out 是一款面向学生和研究团队的论文项目管理应用，帮助你从选题到提交，全流程管理论文写作。

## 功能特性

### 📋 进度追踪
- **任务看板** - 拖拽式任务管理，清晰掌握进度
- **甘特图** - 时间线视图，规划写作节奏
- **里程碑** - 关键节点提醒，不错过任何 deadline
- **统计分析** - 可视化进度报告

### 📚 文献管理
- **多种导入方式** - 支持 BibTeX、DOI、手动录入
- **智能分类** - 标签系统，灵活组织文献
- **阅读笔记** - 记录阅读心得和关键摘录
- **引用生成** - 一键生成 APA、MLA、GB/T 7714 等格式

### ✍️ 写作辅助
- **结构化写作** - 章节树状结构，大纲清晰
- **富文本编辑** - 支持表格、图片、公式、代码块
- **实时协作** - 多人同时编辑，实时同步
- **版本控制** - 完整修改历史，随时回滚

### 📤 导出功能
- **多格式支持** - PDF、Word、LaTeX、Markdown
- **自动参考文献** - 根据引用自动生成文献列表
- **模板定制** - 适配不同学校/期刊要求

## 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | Vue 3 + TypeScript + Vite |
| UI | Element Plus |
| 状态管理 | Pinia |
| 编辑器 | Tiptap |
| 实时协作 | Yjs (CRDT) |
| 后端 | Python FastAPI |
| 数据库 | PostgreSQL |
| 缓存 | Redis |
| 文档转换 | Pandoc |

## 项目结构

```
work-it-out/
├── backend/                 # FastAPI 后端
│   ├── app/
│   │   ├── main.py         # 应用入口
│   │   ├── config.py       # 配置管理
│   │   ├── database.py     # 数据库连接
│   │   ├── models/         # 数据模型
│   │   ├── schemas/        # Pydantic schemas
│   │   ├── api/            # API 路由
│   │   ├── services/       # 业务逻辑
│   │   └── utils/          # 工具函数
│   ├── alembic/            # 数据库迁移
│   ├── tests/              # 测试文件
│   └── requirements.txt
│
├── frontend/               # Vue 3 前端
│   ├── src/
│   │   ├── views/          # 页面组件
│   │   ├── components/     # 功能组件
│   │   ├── stores/         # Pinia 状态
│   │   ├── api/            # API 调用
│   │   ├── composables/    # 组合式函数
│   │   └── types/          # TypeScript 类型
│   ├── package.json
│   └── vite.config.ts
│
├── docker-compose.yml      # Docker 编排
├── PRD.md                  # 产品需求文档
└── README.md               # 项目说明
```

## 快速开始

### 环境要求

- Node.js >= 18
- Python >= 3.11
- PostgreSQL >= 15
- Redis >= 7
- Docker & Docker Compose (推荐)

### 使用 Docker 启动 (推荐)

```bash
# 克隆项目
git clone https://github.com/your-username/work-it-out.git
cd work-it-out

# 启动所有服务
docker-compose up -d

# 访问应用
# 前端: http://localhost:3000
# API: http://localhost:8000
# API 文档: http://localhost:8000/docs
```

### 本地开发

#### 后端

```bash
cd backend

# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件，配置数据库连接等

# 运行数据库迁移
alembic upgrade head

# 启动开发服务器
uvicorn app.main:app --reload --port 8000
```

#### 前端

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build
```

## API 文档

启动后端服务后，访问以下地址查看 API 文档：

- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## 主要 API 端点

| 模块 | 端点 | 描述 |
|------|------|------|
| 认证 | `POST /api/v1/auth/login` | 用户登录 |
| 项目 | `GET /api/v1/projects` | 获取项目列表 |
| 任务 | `GET /api/v1/projects/{id}/tasks` | 获取任务列表 |
| 文献 | `POST /api/v1/projects/{id}/literature/import` | 导入文献 |
| 文档 | `GET /api/v1/documents/{id}/versions` | 获取版本历史 |

## 开发路线图

### MVP (v0.1) - 进行中
- [x] 项目初始化
- [ ] 用户认证系统
- [ ] 项目 CRUD
- [ ] 任务看板
- [ ] 基础文献管理

### v0.2 - 写作功能
- [ ] Tiptap 编辑器集成
- [ ] 文档版本控制
- [ ] 版本对比 (Diff)

### v0.3 - 协作功能
- [ ] 实时协作编辑 (Yjs)
- [ ] 评论系统
- [ ] 通知系统

### v1.0 - 完整版
- [ ] 甘特图视图
- [ ] PDF/Word/LaTeX 导出
- [ ] DOI 自动导入
- [ ] 移动端适配

## 贡献指南

欢迎贡献代码！请遵循以下步骤：

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'Add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 创建 Pull Request

### 代码规范

- 前端：ESLint + Prettier
- 后端：Ruff + Black
- 提交信息：遵循 [Conventional Commits](https://www.conventionalcommits.org/)

## 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

## 联系方式

- 问题反馈：[GitHub Issues](https://github.com/your-username/work-it-out/issues)
- 功能建议：[GitHub Discussions](https://github.com/your-username/work-it-out/discussions)

---

**Work It Out** - 让论文写作不再焦虑 ✨
